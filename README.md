# OS-concurrent-banking-system
A multithreaded banking system simulation in C demonstrating core OS concepts — CPU scheduling (FCFS, Priority, Round Robin), synchronization (mutex/semaphores), deadlock avoidance (Banker's Algorithm), IPC (message queues), and memory management (FIFO/LRU paging).
